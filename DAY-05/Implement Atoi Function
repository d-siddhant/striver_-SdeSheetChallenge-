int atoi(string str) {
    // Write your code here.
    int ans=0;
    for(int i=0;i<str.size();i++){
        if(str[i]>='0' and str[i]<='9') {
            ans=ans*10+(str[i]-'0');
        }
    }
    if(str[0]=='-') return (-1*ans);
    else return ans;
}
