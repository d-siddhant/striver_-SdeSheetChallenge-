int findMinimumCoins(int amount) 
{
    // Write your code here
    int a[]={1, 2, 5, 10, 20, 50, 100, 500, 1000};
    int ans=0;
    for(int i=8;i>=0;i--){
        int p=amount/a[i];
        amount-=(a[i]*p);
        ans+=p;
    }
    return ans;
}
